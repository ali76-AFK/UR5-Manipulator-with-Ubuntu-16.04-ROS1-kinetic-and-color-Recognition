container commands

# Allow local containers to use your X server (only needed once per session)
xhost +local:docker

# Run the container with host networking and X11
docker run --rm -it \
  --net=host \
  --env="DISPLAY" \
  -v /tmp/.X11-unix:/tmp/.X11-unix:rw \
  ur5_kinetic_ur5gazebo


entering from another terminal

docker exec -it 265cafd4147a bash

or 

docker exec -it upbeat_antonelli bash
